---
ntp_enabled: true
ntp_timezone: Europe/Kiev

ntp_manage_config: false

# NTP server area configuration (leave empty for 'Worldwide').
# See: http://support.ntp.org/bin/view/Servers/NTPPoolServers
ntp_area: ''
ntp_servers:
  - "time1.microsoft.com iburst"

ntp_restrict:
  - "127.0.0.1"
  - "::1"
